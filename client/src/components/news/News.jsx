import React, { memo } from 'react'
import HeroCard from '../slider/HeroCard'

const News = memo(() => {
    return (
        <>
            <HeroCard page={"News"} />
        </>
    )
})

export default News